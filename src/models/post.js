export default class Post {
    constructor (id, userId, title, createdDate, modifiedDate) {
      this.id     = id;
      this.userId = userId;
      this.title  = title;
      this.body   = body;
      this.createdDate = createdDate;
      this.modifiedDate = modifiedDate;
    }
  }